apiVersion: v1
kind: Service
metadata:
  namespace: nlp
  labels:
    name: data-node
  name: data-node
spec:
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    name: data-node
status:
  loadBalancer: {}
